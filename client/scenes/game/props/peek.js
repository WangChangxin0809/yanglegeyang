/**
 * 道具：透视
 * 功能待实现，当前仅占位
 */

/**
 * 使用透视道具
 * @param {Object} _state - 游戏状态 { cards, slots }
 * @returns {boolean} 是否成功使用
 */
function use(_state) {
  console.log('[道具] 透视')
  return false
}

module.exports = { use }
